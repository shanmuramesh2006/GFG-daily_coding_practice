class Solution {
    int missingNum(int arr[]) {
        long n=arr.length+1;
        long sum=n*(n+1)/2;
        long arrsum=0;
        for(int i=0;i<arr.length;i++)
        {
            arrsum+=arr[i];
        }
        return (int) (sum-arrsum);
    }
}
